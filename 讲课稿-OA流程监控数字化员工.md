# OA 流程监控数字化员工 —— 教学讲课稿（内容初稿）

> 用途：周四 AI 开发流程讲课 · 待用户确认后生成 Claude 风格可编辑 PPT
> 结构：三幕 + 彩蛋（踩坑实录）+ 演示 + 展望，共 16 页
> 每页含：画面要点（PPT 上放什么）/ 讲稿（口头说什么）/ 可视化建议

---

## 第 1 页 · 封面

**画面要点**
- 主标题：当一个 OA 系统遇上 AI 数字员工
- 副标题：从业务痛点到 AI 落地的完整开发流程实录
- 署名：知识管理中心 · 2026 年 9 月
- 底部一行小字：真实项目 · 真实代码 · 真实数据

**讲稿**
> 今天不讲理论，讲一个我们亲手做完的项目：怎么让一个普通的 OA 系统，长出一个 7×24 小时不睡觉的「AI 监控员工」。整个流程大家可以照着复制。

---

## 第 2 页 · 今天讲什么（议程）

**画面要点**
三幕结构，用三个大数字块：
1. **为什么**——业务痛点与需求场景
2. **靠什么**——我们的 OA 网站与接口层建设
3. **怎么连**——OpenClaw 如何消费接口、形成闭环
- 尾注：附赠：踩坑实录 + 现场演示

**讲稿**
> 三句话概括今天：审批流会「卡死」，AI 能不能发现并催？OA 是老系统，怎么给 AI 开门？OpenClaw 是平台，怎么让它干活？最后我会把开发路上踩的坑全部公开——那些才是最有教学价值的部分。

---

## 第一幕：为什么（需求场景）

## 第 3 页 · 痛点：审批流正在「悄悄卡死」

**画面要点**
- 一个真实数据卡片：**全公司在途审批 30 单，其中 6 单停留超过 48 小时**
- 一张「单据生命周期」示意：提交 → [审批中⏳] → 通过/拒绝，中间的⏳没人看
- 三条痛点：
  1. 看不见——没人知道哪些单卡住了、卡在谁手里
  2. 没人管——审批人忘了，申请人不好意思催
  3. 查不动——人工统计要翻数据库，老板要一份周报得手工做半天

**讲稿**
> 我们的 OA 里躺着 30 张正在审批的单子，其中 6 张已经停了超过两天。没有任何人会收到提醒——申请人以为领导在忙，领导压根忘了。这就是痛点：不是系统坏了，而是**流程缺少一双眼睛**。

---

## 第 4 页 · 需求方案：雇一个不睡觉的数字员工

**画面要点**
- 概念图：一个「AI 数字员工」形象（我们叫它**小奥**），站在 OA 流程旁边 7×24 巡检
- 四项职责卡片：
  1. **巡检**——每小时扫一遍在途审批
  2. **预警**——超时了就分级报警
  3. **催办**——给审批人发催促，抄送上级
  4. **报告**——每周自动发布审批健康度公告
- 来源标注：出自《知识管理中心-OA流程监控看板》方案

**讲稿**
> 需求方的原话是：能不能让 AI 帮我们盯着审批流？翻译成技术语言就是四件事：巡检、预警、催办、报告。注意这四件事里，**判断和说话交给 AI，动作和数据留在系统里**——这个分工原则贯穿了整个开发。

---

## 第 5 页 · 核心规则：分级预警怎么设计

**画面要点**
- 一条时间轴，标注三道闸门：
  - **24h**：停留超一天 → 催审批人，第 1 次
  - **48h**：累计催满 3 次 → 升级，抄送上级
  - **48h 无动作**：判定「疑似无需审批」→ 标注上报，人工裁决
- 强调框：高风险审批人（连续两月霸榜）→ 建议移出审批节点（二期）

**讲稿**
> 预警规则是业务定的，但有个关键设计：**48 小时还不动，AI 只「标注」，不「砍单」**。为什么？自动改单据是不可逆动作，AI 可以当哨兵，但不能当刽子手。这个边界是业务和技术一起确认的——AI 开发第一课：**搞清楚哪些事让 AI 做，哪些事必须留给人**。

---

## 第二幕：靠什么（我们的 OA 与接口层）

## 第 6 页 · 底座：这是一个什么样的 OA

**画面要点**
- 架构图（用现成的 Canvas 架构图改绘）：
  ```
  浏览器 / OpenClaw
      ↓
  樱花 Frp 内网穿透（外网入口 :20857）
      ↓
  Docker：nginx → PHP8.2(ThinkPHP) → MariaDB
  ```
- 三个标签：开源勾股 OA 深度定制 · Docker 一键部署 · 内网穿透对外开放
- 数据小卡片：62 名员工 · 19 类审批流程 · 30 张在途单

**讲稿**
> 底座很朴素：一个跑在 Docker 里的开源 OA，三个容器（nginx、PHP、数据库），公司内网穿透出去一个入口。**不神秘、不高级，但够用**——这是我想传递的第二个观点：AI 落地不需要先换系统，老系统也能接入 AI。

---

## 第 7 页 · 关键问题：AI 怎么「进门」？

**画面要点**
- 左右对比：
  - ❌ **给人用的**：网页 + 按钮界面，靠浏览器 Session 登录
  - ✅ **给 AI 用的**：接口 + JSON 数据，靠 JWT Token 通行证
- 中间结论：**必须新建一层「AI 专用接口」**
- 数字高亮：**7 个接口，1 天开发，全部实测通过**

**讲稿**
> AI 不长眼睛，它读不懂漂亮的审批页面。它需要的是：一个网址、一份 JSON。原系统 50 多个接口是给网页用的（靠 cookie），AI 调会全被拦。所以我们没有改老代码，而是**加了一层薄薄的专用接口**——这是本次开发唯一动代码的地方。

---

## 第 8 页 · 接口设计：7 个接口 = AI 的全部工具箱

**画面要点**
- 一张表（每个接口一行，配图标）：

| 接口 | 干什么 | 比喻 |
|---|---|---|
| `token` | 账号密码换通行证 | 刷工卡 |
| `ping` | 心跳 + 在途总览 | 探个头 |
| `approve_pending` ⭐ | 在途单+停留时长+审批人+风险 | 巡检主力 |
| `approve_stats` | 按人/按类型统计 | 体检报告原料 |
| `todo_list` | 某人的待办 | 点名 |
| `message_send` | 发催办站内信 | 喇叭 |
| `note_publish` | 报告发成公告 | 黑板报 |

- 高亮 `approve_pending` 的返回示例片段（JSON，标注关键字段 stay_hours / risk / check_users）

**讲稿**
> 7 个接口不多，但设计有讲究：**读的多、写的少**。5 个只读查询，2 个写操作（发消息、发公告）也都是「催一催、喊一喊」级别的安全动作。自动砍单那种接口我们压根没做——工具箱里不放进危险的刀。

---

## 第 9 页 · 安全设计：一张有边界的工卡

**画面要点**
- JWT 流程图：账号密码 → `token` 接口 → 通行证（1 小时有效）→ 之后每个请求带头
- 专用账号卡片：**小奥（aiworker）**，uid 61，全公司视野，只属于 AI
- 三条安全原则：
  1. AI 用独立账号，不用任何人的账号
  2. Token 一小时过期，泄露影响可控
  3. 密码走私密渠道配置，不进代码不进群

**讲稿**
> 给 AI 发的是**专用工卡**，不是管理员密码。它有全公司视野（巡检需要），但只能做这 7 件事。哪怕 Token 被泄露，作废时间最多一小时。AI 开发第三课：**最小权限原则，从第一行代码就开始**。

---

## 第三幕：怎么连（OpenClaw 实现闭环）

## 第 10 页 · 一次完整的巡检闭环

**画面要点**
- 全流程图（时序）：
  ```
  OpenClaw 定时触发（每小时）
    → token 换通行证
    → approve_pending 拉在途单
    → AI 判断：谁超时了？催过几次？（状态机在 OpenClaw 侧）
    → message_send：催审批人 + 抄送上级（一条消息两人收）
    → 周五：approve_stats 汇总 → AI 写报告 → note_publish 发全员公告
  ```
- 标注分工：**OA 只出数据，OpenClaw 管脑子**

**讲稿**
> 注意一个关键设计：接口是**无状态**的。「这单催过几次」这种记忆，OA 不存，OpenClaw 自己记（本地文件或数据库）。这样 OA 侧极简，AI 侧规则随便改——业务哪天说 24 小时改成 12 小时，改 AI 配置就行，OA 一行代码不用动。

---

## 第 11 页 · 抄送上级与「疑似无需审批」

**画面要点**
- 左：催办消息的 to_uids = "审批人,上级"，一条消息双送达（附真实调用示例）
- 右：48h 判定流程：AI 标注 → 通知 Owner（饶旺）→ **人工裁决**，单据状态不动
- 附录说明：全员「部门→上级」映射表已导出给 AI 使用

**讲稿**
> 业务确认的触达规则是「催本人、抄上级」。实现上就是接口参数里多传一个 uid，零开发。而 48 小时不动的情况，AI 只标注「疑似无需审批」并上报，**最终按钮仍在人手里**。AI 是哨兵，不是法官。

---

## 第 12 页 · 试点：用数据选战场

**画面要点**
- 近 30 天真实数据表：

| 审批类型 | 30 天单量 | 在途 | 平均耗时 |
|---|---|---|---|
| 请假 | 25 | 8 | 23.6h |
| 报销 | 5 | **5（全部滞留）** | 无一完结 |
| 会议室预定 | 5 | 4 | 23h |

- 结论：试点参数 `type=leaves,expense,meetings`；报销全滞留 = 最需要监控的类型

**讲稿**
> 试点范围不是拍脑袋，是**先用统计接口跑了真实数据**：请假量最大，报销居然 5 单全部在途没有一单批完——这就是 AI 该上岗的证据。做 AI 项目，先让数据说话。

---

## 第 13 页 · 彩蛋：我们踩过的四个坑（最有含金量的一页）

**画面要点**
四个坑，每个一格「症状 → 根因 → 解法」：
1. **AI 调接口被「请先登录」拦截** —— 老代码的 Session 检查先于 JWT 执行 → 新接口层跳过 Session（教训：鉴权测试必须无 cookie 干净环境）
2. **参数悄悄失效** —— nginx 配置丢了 `$args`，`?days=7` 全被吞，接口不报错只是「假装正常」→ 补上重载（教训：「能用」≠「参数生效」，每个参数要实测）
3. **本机正常 ≠ 外网能访问** —— 服务器怎么测都通，OpenClaw 全超时 → 分层排查法（代理→端口→域名→服务）定位到出口策略
4. **链路间歇抖动** —— 内网穿透节点一天断 41 次，6 连测失败 1 次 → AI 侧加重试 3 次，成功率 83%→99.5%（教训：**AI 调用外部系统，重试是必选项**）

**讲稿**
> 这一页比前面所有页都值钱。四个坑覆盖了 AI 对接真实系统的四大经典死法：鉴权、参数、网络、稳定性。每一条我们都付过时间成本，今天免费送给大家。

---

## 第 14 页 · 现场演示

**画面要点**
- 演示清单（3 个动作）：
  1. OpenClaw 说一句话 → 拉出全公司在途审批（risk=high 的 6 单高亮）
  2. 让 AI 催一张真实的滞留单 → 切 OA 页面看站内信到达
  3. 让 AI 出一份本周健康度报告 → 公告栏实时出现
- 兜底预案小字：若现场网络抖动，用本地 127.0.0.1:8010 链路演示

**讲稿**
> 下面不念 PPT 了，直接看真系统跑。三个动作：查、催、报。如果现场网络抖（前面讲过，我们的链路有 17% 概率抽风），我就切本地链路，效果一样。

---

## 第 15 页 · 复盘：这套流程可以复制到任何系统

**画面要点**
- 通用五步法（横向流程条）：
  **① 翻译需求**（业务话→技术话，明确 AI 做什么/不做什么）
  → **② 开薄接口**（只读优先，写操作最小化，独立 AI 账号）
  → **③ 无状态设计**（规则记忆放 AI 侧，系统侧极简）
  → **④ 分层联调**（鉴权→参数→网络→稳定性，逐层实测）
  → **⑤ 小步试点**（用数据选范围，跑通再放大）
- 一句话总结：**系统出数据，AI 出判断，人出决定**

**讲稿**
> 今天讲的不是 OA，是一套方法论。任何老系统——CRM、ERP、工单——想接 AI，都是这五步。记住那句话：系统出数据，AI 出判断，人出决定。边界画清楚，AI 落地就不玄了。

---

## 第 16 页 · 展望与致谢

**画面要点**
- 二期路线图（三个小节点）：审批写操作（带人工确认）→ 健康度看板可视化 → 更多流程接入
- Q&A 引导
- 致谢：知识管理中心 / OpenClaw 平台团队

**讲稿**
> 一期我们守住了边界：AI 只动嘴不动手。二期才考虑让它「动手」——自动砍单、移出审批人，每一个写操作都会配人工确认。谢谢大家，欢迎提问。

---

## 附：给 PPT 生成的备注（确认内容后执行）

- **风格**：Claude 官方风格——米白/奶油底色、衬线大标题（类 Tiempos）、赤陶橙/深灰强调色、大留白、卡片化布局
- **可编辑要求**：全部元素为原生文本框/形状（非图片），现场可改数字和名单
- **图示复用**：第 6 页架构图可用 Obsidian Canvas 那张改绘；第 10 页闭环图可用流程图
- **数据口径**：所有数字（30 单/6 高危/Top3 类型）均来自 2026-09-09 实测，讲课前一天建议重跑一遍刷新
