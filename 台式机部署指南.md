# GoldLogic OA 本地部署指南（Docker 版）

> 把这套演示 OA 完整搬到**任意一台电脑**上本地运行。前提只有一个：装好 Docker Desktop。全程 20 分钟，其中 15 分钟是等下载。

## 这套系统是什么

| 组成 | 说明 |
|---|---|
| 勾股OA（深度改造版） | PHP 8.2 + ThinkPHP 6 + MariaDB 10.11，已改造成"冠林集团 / GoldLogic"品牌 |
| 62 名演示员工 | 完整组织架构、职位、权限组、19 类审批流（详见《演示账号说明.md》） |
| 演示业务数据 | 客户/合同/项目/审批/会议室/日志/发票等，工作台统计非零 |
| AI 数字员工接口 | `app/api/controller/Ai.php`，7 个 JWT 接口，供 OpenClaw 等 AI 平台对接（见《OA-AI接口说明.md》） |
| 全量数据库种子 | `db_seed_goldlogic.sql`，**首次启动自动导入**，无需手动操作 |

## 架构一览

```
浏览器/AI ──→ http://127.0.0.1:8010
                │
        nginx 容器 (8010→80)
                │ FastCGI
        php 容器 (ThinkPHP，代码目录直接挂载)
                │
        mariadb 容器 (3307→3306，数据存 docker 卷 dbdata)
```

## 部署三步走

### 第 1 步：装 Docker Desktop（已装可跳过）

下载地址：https://www.docker.com/products/docker-desktop/

安装后启动它，等任务栏鲸鱼图标变绿（Windows 家庭版需先开启 WSL2，安装器会自动提示）。

国内拉镜像慢的话，在 Docker Desktop → Settings → Docker Engine 里加镜像加速：

```json
{
  "registry-mirrors": [
    "https://docker.m.daocloud.io",
    "https://dockerproxy.net"
  ]
}
```

### 第 2 步：clone 仓库

```bat
git clone https://github.com/xgl1103/GoldLogic-OA.git
cd GoldLogic-OA
```

> 没装 git 的话，在 GitHub 仓库页点绿色 Code 按钮 → Download ZIP，解压后进入目录也一样。

### 第 3 步：双击 `启动网站.bat`

脚本会自动：检查 Docker 引擎 → `docker compose up -d`（首次会拉镜像+建库+导入种子，约 5-15 分钟）→ 打开浏览器登录页。

**首次启动请耐心等种子导入完成**（约 1 分钟），然后访问：

```
http://127.0.0.1:8010/home/login/index.html
```

| 账号 | 密码 | 用途 |
|---|---|---|
| `admin` | `GoldLogic@2026` | 超级管理员 |
| `aiworker` | `Ai@YHLG66TqtZYg5h` | AI 数字员工「小奥」，调 /api/ai/* 接口用 |

其余 60 名员工账号见《演示账号说明.md》（密码统一 `GoldLogic@2026`）。

## 日常使用

| 操作 | 方式 |
|---|---|
| 启动 | 双击 `启动网站.bat`（Docker Desktop 开着就行） |
| 停止 | 双击 `停止网站.bat`（数据保留） |
| 彻底重置数据库 | `docker compose down -v` 再 `docker compose up -d`（回到种子初始状态） |
| 改代码 | 直接改 `gouguoa/` 下的 PHP 文件，刷新即生效（代码是挂载进容器的） |

## 更新代码的日常流程

在这台开发机改完代码后：

```bash
git add -A && git commit -m "xxx" && git push
```

目标电脑上：

```bat
git pull
```

> 改了 PHP 代码后如果页面没变化，删 `gouguoa/runtime/` 下对应模块的 temp 缓存即可；改了种子 SQL 需要重建库（`docker compose down -v`）。

## 常见问题

| 症状 | 原因 | 解决 |
|---|---|---|
| 打开 8010 拒绝连接 | 容器没起来 / 首次还在导种子 | `docker compose ps` 看三个容器是否都 Up；首次等 1 分钟 |
| 打开 502 | php 容器没就绪 | 等半分钟刷新；`docker logs goldlogic-php` 看报错 |
| 登录报密码错误 | 种子还没导完 | `docker logs goldlogic-mariadb` 看到 "ready for connections" 且无导入报错后重试 |
| 端口 8010/3307 被占用 | 本机其他程序占了 | 改 `docker-compose.yml` 里 `ports`（如 `"8020:80"`）后重启 |
| 页面样式乱 | 浏览器缓存 | `Ctrl+F5` 强刷 |
| Docker Desktop 起不来 | WSL2 未启用 | 按提示安装 WSL2，或 BIOS 开虚拟化 |

## 关于旧的原生进程版

仓库里的 `【旧·进程版】启动/停止OA.bat`、`install.bat` 是**已弃用**的原生部署方案（PHP/MariaDB/nginx 直接装 Windows 上），保留仅作历史参考，**新电脑不要用**，统一走 Docker。
