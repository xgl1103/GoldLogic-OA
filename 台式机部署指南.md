# GoldLogic OA 台式机部署指南

> 把这套演示 OA 完整搬到你自己的台式机上运行。全程 **30 分钟内**，其中 20 分钟是等下载。

## 这套系统是什么

| 组成 | 说明 |
|---|---|
| 勾股OA（深度改造版） | PHP 8.2 + ThinkPHP 6 + MariaDB 的传统 OA 系统，已改造成"冠林集团 / GoldLogic"品牌 |
| 18 名演示员工 | 完整组织架构、职位、权限组、审批流（详见《演示账号说明.md》） |
| 演示业务数据 | 客户/合同/项目/审批/会议室/日志/发票等约 120 条，工作台统计非零 |
| 全量数据库导出 | `db_seed_goldlogic.sql`（380KB），clone 下来就有，启动脚本自动导入 |

## 为什么必须按固定路径安装

所有配置文件（php.ini、验证码字体、nginx、MariaDB）里写死的都是这套绝对路径：

```
C:\Code\oa-env\          ← PHP、MariaDB、nginx、字体 都住这里
C:\Code\OA网站\          ← 本仓库 clone 到这里（建议，非强制）
```

> 类比：所有家具的安装螺丝孔都是按这个房间的尺寸打好的，换个房间就得重新打孔。**clone 到别的盘符也能跑，但要手动改 php.ini / captcha.php / my.ini 里的路径**，何必呢。

## 部署三步走

### 第 1 步：clone 仓库

```bat
mkdir C:\Code
cd C:\Code
git clone https://github.com/xgl1103/GoldLogic-OA.git OA网站
cd OA网站
```

### 第 2 步：双击 `install.bat`

自动完成：

1. 下载 PHP 8.2 NTS 便携版（约 30MB）→ 解压到 `C:\Code\oa-env\php`，并放入配好的 `php.ini`
2. 下载 MariaDB 10.11.10（约 70MB）→ 解压到 `C:\Code\oa-env\mariadb`，并放入 `my.ini`
3. 布置验证码字体（`env\fonts` → `C:\Code\oa-env\fonts`）和 nginx（`env\nginx-1.26.3`）

> 如果公司网络下不动，手动下载这两个包后按提示路径解压，再重跑 install.bat：
> - PHP: `https://windows.php.net/downloads/releases/archives/php-8.2.29-nts-Win32-vs16-x64.zip`
> - MariaDB: `https://archive.mariadb.org/mariadb-10.11.10/winx64-packages/mariadb-10.11.10-winx64.zip`

### 第 3 步：双击 `启动OA.bat`

自动完成（每次开机后运行这一个就行）：

1. 启动 MariaDB（首次运行会自动初始化数据目录）
2. 首次运行自动导入 `db_seed_goldlogic.sql`（建库 + 全部演示数据）
3. 启动 3 个 php-cgi 进程 + nginx（端口 8010）
4. 自动打开浏览器

**验证成功**：看到冠林集团登录页，用 `admin / GoldLogic@2026` 登录。

## 停止与开机自启

| 操作 | 方式 |
|---|---|
| 停止全部服务 | 双击 `停止OA.bat` |
| 开机后启动 | 双击 `启动OA.bat`（已运行的组件会自动跳过，可重复点） |
| 数据库数据 | 存在 `C:\Code\oa-env\mariadb\data`，删除 = 清空全部数据 |

## 内网穿透注意事项（重要）

台式机是你自己的设备，可以做穿透，但请记住：

1. **数据全是虚构的**（冠林集团不存在），泄露无实际危害——但登录密码别用你常用的密码
2. 推荐用 cpolar（国内直连，注册后有免费隧道）：

```bat
cpolar http 8010
```

生成的 `https://xxxx.cpolar.io` 网址就是公网入口。免费版网址每次重启会变，演示前先跑一次拿到新网址发给学员。

3. **密码已统一为 `GoldLogic@2026`**，公网演示时如果介意，登录 admin 后到系统设置改掉

## 更新代码的日常流程

在公司开发机改完代码后：

```bash
git add -A && git commit -m "xxx" && git push
```

台式机上：

```bat
cd C:\Code\OA网站
git pull
```

**注意**：改 PHP 代码后要清模板缓存才生效（或等它自动失效）：

```bat
del /q C:\Code\OA网站\gouguoa\runtime\home\temp\*.php
```

改了**数据库结构/种子数据**（公司代码里改的是 PHP，一般不涉及），需要重新导出 dump 并在台式机上重建库——这种时候叫 AI 帮你做，别手搓 SQL。

## 常见问题

| 症状 | 原因 | 解决 |
|---|---|---|
| 打开是 502 | php-cgi 没起来 | 任务管理器看有没有 3 个 php-cgi.exe；重跑 `启动OA.bat` |
| 打开是拒绝连接 | nginx 没起来 | 重跑 `启动OA.bat`（不要重复点 nginx start，先 `停止OA.bat` 再启） |
| 登录报"用户或密码错误"但密码没错 | 数据库没起来 | 看 3306 端口：`netstat -ano | findstr 3306`；重跑 `启动OA.bat` |
| 验证码不显示 | 字体没布置 | 确认 `C:\Code\oa-env\fonts\4.ttf` 存在；重跑 `install.bat` |
| 网页样式乱 | 缓存 | `Ctrl+F5` 强刷 |
| PHP 报"找不到扩展" | php.ini 没复制 | 确认 `C:\Code\oa-env\php\php.ini` 存在；重跑 `install.bat` |

## 账号速查（密码统一 `GoldLogic@2026`）

| 账号 | 姓名 | 角色 | 演示看点 |
|---|---|---|---|
| `admin` | 超级员工 | 超级管理员 | 全公司待办+可代签任何审批 |
| `raowang` | 饶旺 | 董事长 | 高管视角待办（用车/合同审批） |
| `lisi` | 李四 | 人事专员 | 大量待审单（报销/合同/借支/用章） |
| `zhaoliu` | 赵六 | 财务总监 | 收付款终审 |
| `chenjiatong` | 陈佳彤 | CFO | 报销终审环节 |

完整 18 人名单、组织架构、权限组见《演示账号说明.md》。
